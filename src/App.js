import React from 'react';
import Client from "./game/Client";

const style = {
  display: 'flex',
  justifyContent: 'center'
};

export const App = () => (
  <div style={style}>
    <Client></Client>
  </div>
)
